# SPEC — Workspace Ειδικού Παιδαγωγού

Αυτό το αρχείο είναι η πηγή αλήθειας του project για τις **σταθερές αρχές και τους κανόνες του
προϊόντος** — τι είναι η εφαρμογή, φιλοσοφία UX, απόρρητο, οι βασικές έννοιες του data model,
πολιτική testing, κανόνες συνεργασίας. Κάθε νέα συνομιλία με AI assistant ξεκινά με ανάγνωση αυτού
του αρχείου.

Αυτό το αρχείο δεν είναι πλέον ο τόπος όπου καταγράφεται «τι υπάρχει σήμερα» ή ιστορικό sprints —
ακριβώς αυτή η προσπάθεια ήταν που το άφησε στο παρελθόν να μείνει πίσω από την πραγματικότητα.
Ενημερώνεται σπάνια, σκόπιμα, μόνο όταν αλλάζει μια πραγματική αρχή/κανόνας.

## Σχέση με τα άλλα έγγραφα

- **`DESIGN_SYSTEM.md` / `COMPONENT_GUIDE.md`** — πηγή αλήθειας για UI/UX παρουσίαση (χρώματα,
  τυπογραφία, spacing, εικονίδια, sidebar/navigation, tabs, responsive συμπεριφορά). Σε σύγκρουση
  σχεδιασμού υπερισχύουν αυτά.
- **`docs/release-1.0-report.html`** — πηγή αλήθειας για **«τι υπάρχει πραγματικά σήμερα»**:
  πλήρης λίστα modules, λειτουργική κάλυψη, known limitations/technical debt, production
  checklist. Ανανεώνεται σε κάθε release (v1.0, v1.1, …) — όχι συνεχώς. Σε σύγκρουση σχετικά με
  το τι είναι ήδη υλοποιημένο, υπερισχύει αυτό, όχι ένα παλιωμένο σημείο εδώ.
- **`docs/history.md`** — παγωμένο ιστορικό αρχείο (τα αρχικά βήματα υλοποίησης + το sprint log
  μέχρι το Sprint 6). Δεν ενημερώνεται πλέον, δεν είναι πηγή αλήθειας για τίποτα τρέχον.
- **Αυτό το SPEC.md** — πηγή αλήθειας για ό,τι ΔΕΝ είναι οπτική παρουσίαση ούτε τρέχουσα
  κατάσταση υλοποίησης: λειτουργικότητα, βασικές έννοιες data model, κανόνες αξιολόγησης/
  μέτρησης, backup/restore, απόρρητο. Σε σύγκρουση λειτουργικότητας/αρχών υπερισχύει αυτό.

Σχετικά με εικονίδια: όπου αυτό το έγγραφο χρησιμοποιεί emoji (π.χ. 🟢/🟡/🔴 για κατάσταση συνεδρίας, ⭐ για milestone, 🎯/📅/📄 στο παράδειγμα του Timeline), αυτά περιγράφουν την ΈΝΝΟΙΑ της κατάστασης/του τύπου γεγονότος — δεν εντολοδοτούν συγκεκριμένο emoji-icon στο UI. Το πραγματικό οπτικό εικονίδιο (Lucide) καθορίζεται από το DESIGN_SYSTEM.md. Υπάρχοντα emoji στο UI ή σε αποθηκευμένο περιεχόμενο μπορούν προσωρινά να παραμείνουν· δεν αλλάζει το data model μόνο και μόνο για να αφαιρεθούν, και δεν προστίθενται νέα emoji ως UI icons.

## Τι φτιάχνουμε

Web εφαρμογή (PWA) για ειδικούς παιδαγωγούς στην Κύπρο. Ο εκπαιδευτικός καταγράφει μαθητές και στόχους, μετά από κάθε μάθημα σημειώνει με ελάχιστες κινήσεις την επίδοση κάθε μαθητή ανά στόχο, και στο τέλος της περιόδου η εφαρμογή παράγει προσχέδιο έκθεσης προόδου με πραγματικά δεδομένα.

**Γλώσσα διεπαφής: Ελληνικά.** Σχεδίαση mobile-first και tablet-first (ο εκπαιδευτικός καταγράφει κυρίως από κινητό/tablet μέσα στην τάξη). Υπάρχει και προσαρμοσμένο desktop layout — καμία οθόνη δεν σχεδιάζεται desktop-only. Το ακριβές responsive navigation ανά μέγεθος οθόνης ορίζεται στο DESIGN_SYSTEM.md.

## Τεχνολογία (κλειδωμένη — μην προταθούν εναλλακτικές)

- React + Vite
- Αποθήκευση: IndexedDB μέσω Dexie.js — **local-first**: όλα τα δεδομένα ζουν εξ ορισμού αποκλειστικά στη συσκευή
- PWA (service worker, manifest) ώστε να δουλεύει offline και να εγκαθίσταται σε κινητό
- Deploy: GitHub Pages → η εφαρμογή σερβίρεται στο **`learning-fast1.github.io/workspace`**. Ρυθμίσεις: Vite `base: '/workspace/'`, hash-based routing (HashRouter) για να δουλεύουν τα refresh/deep links στο GitHub Pages, PWA manifest με `start_url`/`scope` προσαρμοσμένα στο `/workspace/`.
- Χωρίς localStorage για δεδομένα μαθητή (μόνο IndexedDB). Ένα μικρό αριθμό μη-δεδομένων bootstrap/auth flags (π.χ. sync-authorization hint πριν το άνοιγμα της βάσης) χρησιμοποιούν localStorage — τεκμηριωμένα στο `src/migration/`, ποτέ δεν κρατούν περιεχόμενο μαθητή.
- Export/Import όλων των δεδομένων σε αρχείο JSON (backup)

**Προαιρετική στρώση λογαριασμού/cloud sync (υλοποιημένη):** ο εκπαιδευτικός μπορεί προαιρετικά να
συνδεθεί με λογαριασμό (email + OTP) και να ενεργοποιήσει συγχρονισμό πολλαπλών συσκευών μέσω
Dexie Cloud. Είναι **off by default** (πίσω από feature flag/env var — απουσία του σημαίνει η
εφαρμογή λειτουργεί ακριβώς όπως αμιγώς τοπική) και η ενεργοποίηση συγχρονισμού απαιτεί ρητή,
χειροκίνητη επιβεβαίωση εξουσιοδότησης από τον εκπαιδευτικό (βλ. §Απόρρητο). Όλη η business
λογική της εφαρμογής δουλεύει πάνω στην τοπική βάση (Dexie) ανεξάρτητα — το στρώμα συγχρονισμού
είναι πρόσθετο, δεν την αντικαθιστά.

## Απόρρητο

Οι μαθητές καταχωρούνται με κωδικό (π.χ. "Μ1") και προαιρετικό μικρό όνομα. Δεν ζητούνται πλήρη στοιχεία ταυτότητας.

Η εφαρμογή είναι **local-first**: εξ ορισμού, όλα τα δεδομένα μένουν αποκλειστικά στη συσκευή, χωρίς καμία αυτόματη αποστολή πουθενά. Ο εκπαιδευτικός μπορεί προαιρετικά να ενεργοποιήσει cloud συγχρονισμό (Dexie Cloud) — η ενεργοποίηση απαιτεί ρητή, χειροκίνητη επιβεβαίωση ότι διαθέτει την απαραίτητη εξουσιοδότηση σύμφωνα με την πολιτική του σχολείου/οργανισμού του. Μόνο εφόσον ενεργοποιηθεί έτσι, δεδομένα μαθητών μεταφέρονται από τη συσκευή προς την υποδομή του Dexie Cloud.

Η εφαρμογή δεν παρέχει σήμερα zero-knowledge ή end-to-end κρυπτογράφηση για αυτόν τον συγχρονισμό. Αυτό παραμένει μελλοντικός στόχος (βλ. Roadmap στο `docs/release-1.0-report.html`) πριν ο συγχρονισμός θεωρηθεί κατάλληλος για ευρεία χρήση με ευαίσθητα δεδομένα ειδικής αγωγής.

## Φιλοσοφία UX — Η ΣΥΝΕΔΡΙΑ ΕΙΝΑΙ ΤΟ ΚΕΝΤΡΟ (κρίσιμο — διαβάζεται πριν από κάθε UI απόφαση)

Το data model παρακάτω περιγράφει τα δεδομένα. Η εφαρμογή όμως ΔΕΝ οργανώνεται γύρω από τις οντότητες — οργανώνεται γύρω από τη στιγμή «έχω τώρα μάθημα». Ο εκπαιδευτικός δεν σκέφτεται "Student → Goal → Measurement"· σκέφτεται: έχω μάθημα → ποια παιδιά → τι δουλεύουμε → πατάω → τέλος.

**Teaching Mode — το κεντρικό flow της εφαρμογής:**

1. Ανοίγω την εφαρμογή. Η αρχική οθόνη είναι ένα dashboard (μορφή/layout όπως ορίζει το DESIGN_SYSTEM.md) με σαφείς quick actions — μεταξύ αυτών, **Ατομικό** και **Ομαδικό** παραμένουν άμεσα διαθέσιμες, καθαρές επιλογές που ξεκινούν αμέσως το Teaching Mode.
2. Πατάω π.χ. Ομαδικό → εμφανίζεται λίστα μαθητών με checkboxes → τσεκάρω ☑ Μ1 ☑ Μ2 ☑ Μ4 → πατάω **Έναρξη**.
3. Η εφαρμογή μπαίνει σε **Teaching Mode**: πλήρης οθόνη, ΜΟΝΟ οι ενεργοί στόχοι των επιλεγμένων μαθητών (πρώτα οι high priority), μεγάλα κουμπιά καταχώρησης με ένα tap. Τίποτα άλλο — κανένα μενού, καμία πλοήγηση, καμία περίσπαση. Διακριτό κουμπί ➕ Παρατήρηση πάντα προσβάσιμο.
4. Τελειώνει το μάθημα → πατάω **Τέλος** → επιλέγω διάρκεια (ένα tap) → η συνεδρία αποθηκεύεται με ό,τι καταχωρήθηκε. Ό,τι δεν προλάβαινα να μετρήσω, απλώς δεν μετρήθηκε — καμία υποχρεωτική συμπλήρωση.

Κριτήριο επιτυχίας του Teaching Mode: να είναι ΠΙΟ ΓΡΗΓΟΡΟ από χαρτί και στυλό. Κάθε σχεδιαστική απόφαση μέσα στο Teaching Mode κρίνεται με αυτό το μέτρο.

**ΑΠΑΡΑΒΑΤΟΣ ΚΑΝΟΝΑΣ ΑΠΛΟΤΗΤΑΣ:** Μέγιστο 4 στόχοι ορατοί στην οθόνη ταυτόχρονα — ΠΟΤΕ περισσότεροι, ανεξάρτητα από το πόσους ενεργούς στόχους έχει ο μαθητής. Οι υπόλοιποι με swipe (οριζόντια σελιδοποίηση με ένδειξη κουκκίδων). Σειρά: πρώτα οι high priority. Σε ομαδική συνεδρία: μία «κάρτα» ανά μαθητή, swipe μεταξύ μαθητών — όχι όλοι σε ένα πλέγμα στο κινητό. Κανένας counter, καμία λίστα σημειώσεων, κανένα στοιχείο που δεν χρειάζεται ΑΥΤΗ τη στιγμή. Αν ένα UI στοιχείο δεν βοηθά την καταχώρηση του επόμενου tap, δεν ανήκει στο Teaching Mode.

Η διαχείριση μαθητών, στόχων, γραφημάτων και εκθέσεων υπάρχει κανονικά, αλλά είναι το «γραφείο» της εφαρμογής — εκεί πηγαίνεις όταν προετοιμάζεσαι ή απολογίζεσαι, όχι όταν διδάσκεις. Η καρτέλα μαθητή οργανώνεται σε tabs (Στόχοι, Συνεδρίες, Προφίλ, Ενισχυτές, Έκθεση — βλ. DESIGN_SYSTEM.md) αντί για μία συνεχόμενη σελίδα.

## Data model — βασικές έννοιες

Η εφαρμογή οργανώνει τα δεδομένα γύρω από μαθητές, στόχους και συνεδρίες (βλ. Φιλοσοφία UX
παραπάνω) — όχι σε ανεξάρτητες, ισοδύναμες οντότητες. **Το ακριβές, τρέχον σχήμα (όλοι οι
πίνακες, πεδία, indexes, εκδόσεις) ζει αποκλειστικά στο `src/db.js`, με τη λίστα των βασικών
εκπαιδευτικών πινάκων στο `src/migration/migratedTableNames.js` — αυτά τα δύο αρχεία είναι η
ΜΟΝΑΔΙΚΗ πηγή αλήθειας του σχήματος, όχι αυτή η λίστα.** Παρακάτω περιγράφονται οι βασικές
οντότητες σε επίπεδο έννοιας/κανόνα — όχι πλήρες schema, και όχι εξαντλητική λίστα.

- **Μαθητής (Student)** — κωδικός (π.χ. "Μ1") + προαιρετικό μικρό όνομα, όχι πλήρη στοιχεία
  ταυτότητας (βλ. §Απόρρητο). Δομημένο **functional profile** ανά τομέα (checkboxes + ελεύθερες
  παρατηρήσεις, προαιρετικές υπο-ομάδες με τίτλο — π.χ. Ανάγνωση χωρίζεται σε «Γράμματα &
  γραφοφωνημική αντιστοιχία» / «Ανάγνωση κειμένου & κατανόηση»)· ο τομέας Συμπεριφορά περιλαμβάνει
  ρητά δύσκολες/προβληματικές συμπεριφορές ως δομημένες επιλογές, όχι μόνο ελεύθερο κείμενο,
  ακριβώς επειδή αποτελούν αντικείμενο συστηματικής παρακολούθησης. Ξεχωριστό **προφίλ
  ενισχυτών** (preferences) με έξι δομημένες, ελεύθερα επεκτάσιμες λίστες (του αρέσει/δεν του
  αρέσει/ενισχυτές/αγαπημένες δραστηριότητες/triggers/τον ηρεμεί) — καθημερινό εργαλείο, όχι
  αρχείο.
- **Στόχος (Goal)** — ανήκει σε τομέα (σταθερή λίστα, βλ. παρακάτω), έχει baseline/κριτήριο/τύπο
  μέτρησης/προτεραιότητα (high/medium/low) και κατάσταση (active/achieved/revised/archived) με
  πλήρες ιστορικό μεταβάσεων. Λίστες στόχων ταξινομούνται/φιλτράρονται με βάση την προτεραιότητα.
- **Συνεδρία (Session)** — κατάσταση 🟢 completed / 🟡 interrupted / 🔴 notHeld (με λόγο),
  απουσία σημειωμένη ΑΝΑ ΜΑΘΗΤΗ, διάρκεια σε λεπτά. Μια notHeld συνεδρία εξαιρείται ρητά από κάθε
  σημείο που μετρά «πραγματικές» συνεδρίες (dashboard, στατιστικά, έκθεση). Μπορεί να προέλθει από
  το πρόγραμμα εκπαιδευτικού (βλ. παρακάτω) ή να καταχωρηθεί/διορθωθεί χειροκίνητα εκ των υστέρων.
- **Μέτρηση (Measurement)** — ανά στόχο/συνεδρία, τιμή ανάλογα με τον τύπο μέτρησης του στόχου.
- **Παρατήρηση (Observation)** — σημαντικά συμβάντα που ΔΕΝ είναι μετρήσεις στόχων (π.χ. «πρώτη
  φορά…»), με σήμανση milestone. Καταχώρηση < 15 δευτερόλεπτα, διαθέσιμη πάντα (Teaching Mode ΚΑΙ
  καρτέλα μαθητή). Τροφοδοτεί το Timeline του μαθητή και το προσχέδιο έκθεσης.
- **Πρόγραμμα εκπαιδευτικού** — σταθερό εβδομαδιαίο πρόγραμμα με αλλαγές ισχύος από συγκεκριμένη
  ημερομηνία (νέα έκδοση αντί για in-place edit· «διαγραφή» = τερματισμός ισχύος, ποτέ πραγματική
  διαγραφή, ώστε παλιές συνεδρίες να μη χάνουν σημείο αναφοράς), εξαιρέσεις ανά ημερομηνία
  (ακύρωση/μετακίνηση μεμονωμένης εμφάνισης, με στιγμιότυπο τη στιγμή της αλλαγής), και έκτακτα
  γεγονότα ημερολογίου (καθαρά πληροφοριακά). Τροφοδοτεί — ΠΟΤΕ αυτόματα δεν δημιουργεί
  πραγματική συνεδρία — την καθημερινή σειρά «Η μέρα μου». Ανήκει στην ίδια την εκπαιδευτικό, όχι
  σε συγκεκριμένο μαθητή (μοναδική εξαίρεση στον κανόνα «όχι feature-centric πλοήγηση», βλ.
  DESIGN_SYSTEM.md).
- **Πρότυπο τομέα (DomainTemplate)** — προτεινόμενοι τύποι μέτρησης/κριτήρια/παραδείγματα ανά
  τομέα, χρησιμοποιείται για να προ-συμπληρώνει τη φόρμα δημιουργίας στόχου. Μαζί με τα
  functional-profile options, αποτελεί το «παιδαγωγικό περιεχόμενο» της εφαρμογής.
- **Έκθεση (Report)** — persisted προσχέδιο ανά μαθητή/περίοδο (επιβιώνει σε refresh, auto-save),
  με προεπισκόπηση, σήμανση draft/final (ετικέτα, δεν κλειδώνει επεξεργασία), και ιστορικό
  παλαιότερων εκθέσεων.

Υποστηρικτικές οντότητες (σχολικά έτη + συμμετοχή μαθητή ανά έτος, ιστορικό μεταβάσεων στόχου,
κλινική εκτίμηση ανά συνεδρία, προσωπική βιβλιοθήκη προτύπων στόχων του εκπαιδευτικού, ζωντανά
υπολογιζόμενες ειδοποιήσεις, ρυθμίσεις χρήστη όπως το εμφανιζόμενο όνομα) υποστηρίζουν αυτόν τον
πυρήνα — για την πλήρη, τρέχουσα λίστα βλ. `src/db.js`/`docs/release-1.0-report.html`.

**Σταθερή λίστα τομέων (domains):** Λεπτή κινητικότητα, Αδρή κινητικότητα, Προσοχή/Συγκέντρωση, Εκτελεστικές λειτουργίες, Αισθητηριακός τομέας, Φωνολογική ενημερότητα, Ανάγνωση, Γραπτός λόγος, Μαθηματικά, Προφορικός λόγος, Κοινωνικές δεξιότητες, Συναισθηματική ανάπτυξη, Αυτοεξυπηρέτηση, Συμπεριφορά. Μία μόνο πηγή αλήθειας στον κώδικα: το `src/config/domains.js`, όπου κάθε τομέας έχει σταθερό `id` (π.χ. `reading`) και ελληνική `name` για εμφάνιση. Όλα τα δεδομένα (goals, functionalProfile, domainTemplates) αποθηκεύουν το `id`, ποτέ την ελληνική ονομασία, ώστε μελλοντικές μετονομασίες να μην σπάνε παλιές εγγραφές.

## Τι ΔΕΝ φτιάχνουμε ακόμα (μη προστεθούν χωρίς ρητή έγκριση)

- Zero-knowledge / end-to-end κρυπτογράφηση για το cloud sync (βλ. §Απόρρητο) — το προαιρετικό sync υπάρχει ήδη, η κρυπτογράφησή του όχι ακόμα.
- Portal γονέων/θεραπευτών (μελλοντική φάση — προς το παρόν μόνο μελλοντικό "PDF ενημέρωσης")
- Βιβλιοθήκη δραστηριοτήτων, Teacher Bible
- AI λειτουργίες
- Πολλαπλές γλώσσες
- Σχολικό έτος ως πλήρης **πλοηγητική/οργανωτική αρχή** της εφαρμογής (δικό του section στο sidebar, φιλτράρισμα-πρώτα σε κάθε οθόνη) — οι ίδιες οι οντότητες (σχολικά έτη, συμμετοχή μαθητή ανά έτος, μετάβαση στόχων) υπάρχουν ήδη και χρησιμοποιούνται, αλλά μόνο ως υποστηρικτικό μηχανισμό, όχι ως τον τρόπο που οργανώνεται η πλοήγηση.
- Αυτόματη εισαγωγή επίσημων αργιών Κύπρου στο ημερολόγιο — το ίδιο entity ήδη έχει το σωστό σχήμα για ένα μελλοντικό bulk-seed, απλά δεν υπάρχει ακόμα.
- Εξωτερικός συγχρονισμός ημερολογίων (Google Calendar κ.λπ.), drag-and-drop αναδιάταξη στο ημερολόγιο, πολύπλοκα recurrence patterns.
- **Student creation batch-flow** — μετά την αποθήκευση νέου μαθητή, η εφαρμογή πάει στο προφίλ του αντί να επιστρέψει στη λίστα· δημιουργία πολλών μαθητών στη σειρά απαιτεί χειροκίνητη επιστροφή κάθε φορά. Πιθανή λύση: κουμπί «Αποθήκευση και νέος μαθητής».
- **Cross-tab IndexedDB reactivity test** — δεν έχει επιβεβαιωθεί ρητά η συμπεριφορά όταν η εφαρμογή είναι ανοιχτή σε δύο καρτέλες ταυτόχρονα.
- **Πραγματικό multi-device sync test** — το cloud sync είναι υλοποιημένο και ελεγμένο σε single-device σενάρια· καμία πραγματική two-device ή offline→online conflict δοκιμή δεν έχει τρέξει ακόμα ζωντανά.

Για πλήρη, ενημερωμένη λίστα ό,τι λείπει/είναι known limitation βλ. `docs/release-1.0-report.html`.

## Testing

Πολιτική: κάθε νέα λειτουργικότητα συνοδεύεται από αυτοματοποιημένα tests πριν το commit. Unit/
component tests (Vitest) είναι υποχρεωτικά για: υπολογισμούς προόδου, παραγωγή εκθέσεων,
migrations δεδομένων, export/import, και οποιαδήποτε λογική συγχρονισμού/ιδιοκτησίας δεδομένων —
όχι απαραίτητα για απλές φόρμες/οθόνες χωρίς δική τους λογική. Επιπλέον, ένα μικρό, μόνιμο
Playwright production-smoke suite τρέχει σε κάθε deploy (πριν ΚΑΙ μετά τη δημοσίευση) ως τελευταία
γραμμή άμυνας για πράγματα που τα Vitest tests δομικά δεν μπορούν να πιάσουν (πραγματικός browser,
πραγματικό production build/deploy). Δεν αντικαθιστά το Vitest suite ούτε καλύπτει πλήρη end-to-end
ροές — βλ. `docs/release-1.0-report.html` για την τρέχουσα ακριβή κάλυψη.

## Κανόνες συνεργασίας με AI assistant

- Δουλεύουμε ΕΝΑ βήμα τη φορά. Μην υλοποιείς επόμενα βήματα προκαταβολικά.
- Πριν από μεγάλες αλλαγές, εξήγησε σύντομα τι θα κάνεις.
- Κώδικας απλός και σχολιασμένος στα βασικά σημεία — η δημιουργός συντηρεί μόνη της το project.
- Ό,τι κείμενο βλέπει ο χρήστης: στα Ελληνικά.
