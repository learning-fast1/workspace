# SPEC — Workspace Ειδικού Παιδαγωγού (MVP)

Αυτό το αρχείο είναι η πηγή αλήθειας του project. Κάθε νέα συνομιλία με AI assistant ξεκινά με ανάγνωση αυτού του αρχείου.

## Τι φτιάχνουμε

Web εφαρμογή (PWA) για ειδικούς παιδαγωγούς στην Κύπρο. Ο εκπαιδευτικός καταγράφει μαθητές και στόχους, μετά από κάθε μάθημα σημειώνει με ελάχιστες κινήσεις την επίδοση κάθε μαθητή ανά στόχο, και στο τέλος της περιόδου η εφαρμογή παράγει προσχέδιο έκθεσης προόδου με πραγματικά δεδομένα.

**Γλώσσα διεπαφής: Ελληνικά.** Σχεδίαση mobile-first (ο εκπαιδευτικός καταγράφει από κινητό/tablet μέσα στην τάξη).

## Τεχνολογία (κλειδωμένη — μην προταθούν εναλλακτικές)

- React + Vite
- Αποθήκευση: IndexedDB μέσω Dexie.js — ΟΛΑ τα δεδομένα local, κανένας server, κανένα backend, κανένα login **στο MVP**
- PWA (service worker, manifest) ώστε να δουλεύει offline και να εγκαθίσταται σε κινητό
- Deploy: GitHub Pages, σε ξεχωριστό repository με όνομα `workspace` → η εφαρμογή σερβίρεται στο **learning-fast.com/workspace**. Απαραίτητες ρυθμίσεις από το Βήμα 1: Vite `base: '/workspace/'`, hash-based routing (HashRouter) για να δουλεύουν τα refresh/deep links στο GitHub Pages, PWA manifest με `start_url` και `scope` προσαρμοσμένα στο `/workspace/`.
- Χωρίς localStorage για δεδομένα (μόνο IndexedDB)
- Export/Import όλων των δεδομένων σε αρχείο JSON (backup)

**Αρχιτεκτονική-στόχος (Φάση 2 — ΟΧΙ στο MVP):** λογαριασμοί χρηστών (email) + συγχρονισμός πολλαπλών συσκευών με end-to-end κρυπτογράφηση zero-knowledge — τα δεδομένα μαθητών κρυπτογραφούνται στη συσκευή πριν φύγουν, ο server (και ο δημιουργός της πλατφόρμας) βλέπει μόνο emails και κρυπτογραφημένα δεδομένα. Θα χρησιμοποιηθεί δοκιμασμένη υπηρεσία/βιβλιοθήκη (π.χ. Dexie Cloud ή αντίστοιχη) — ποτέ χειροποίητη κρυπτογραφία. ΣΗΜΑΝΤΙΚΟ για την υλοποίηση του MVP: όλη η λογική της εφαρμογής δουλεύει πάνω στην τοπική βάση (Dexie), ώστε το στρώμα συγχρονισμού να προστεθεί αργότερα χωρίς ξαναγράψιμο.

## Απόρρητο

Οι μαθητές καταχωρούνται με κωδικό (π.χ. Μ1) και προαιρετικό μικρό όνομα. Δεν ζητούνται πλήρη στοιχεία ταυτότητας. Τα δεδομένα δεν φεύγουν ποτέ από τη συσκευή.

## Φιλοσοφία UX — Η ΣΥΝΕΔΡΙΑ ΕΙΝΑΙ ΤΟ ΚΕΝΤΡΟ (κρίσιμο — διαβάζεται πριν από κάθε UI απόφαση)

Το data model παρακάτω περιγράφει τα δεδομένα. Η εφαρμογή όμως ΔΕΝ οργανώνεται γύρω από τις οντότητες — οργανώνεται γύρω από τη στιγμή «έχω τώρα μάθημα». Ο εκπαιδευτικός δεν σκέφτεται "Student → Goal → Measurement"· σκέφτεται: έχω μάθημα → ποια παιδιά → τι δουλεύουμε → πατάω → τέλος.

**Teaching Mode — το κεντρικό flow της εφαρμογής:**

1. Ανοίγω την εφαρμογή. Η αρχική οθόνη κυριαρχείται από ένα ερώτημα: **«Τι κάνεις τώρα;»** με δύο μεγάλα κουμπιά: 🟢 Ατομικό / 🟣 Ομαδικό.
2. Πατάω π.χ. Ομαδικό → εμφανίζεται λίστα μαθητών με checkboxes → τσεκάρω ☑ Μ1 ☑ Μ2 ☑ Μ4 → πατάω **Έναρξη**.
3. Η εφαρμογή μπαίνει σε **Teaching Mode**: πλήρης οθόνη, ΜΟΝΟ οι ενεργοί στόχοι των επιλεγμένων μαθητών (πρώτα οι high priority), μεγάλα κουμπιά καταχώρησης με ένα tap. Τίποτα άλλο — κανένα μενού, καμία πλοήγηση, καμία περίσπαση. Διακριτό κουμπί ➕ Παρατήρηση πάντα προσβάσιμο.
4. Τελειώνει το μάθημα → πατάω **Τέλος** → επιλέγω διάρκεια (ένα tap) → η συνεδρία αποθηκεύεται με ό,τι καταχωρήθηκε. Ό,τι δεν προλάβαινα να μετρήσω, απλώς δεν μετρήθηκε — καμία υποχρεωτική συμπλήρωση.

Κριτήριο επιτυχίας του Teaching Mode: να είναι ΠΙΟ ΓΡΗΓΟΡΟ από χαρτί και στυλό. Κάθε σχεδιαστική απόφαση μέσα στο Teaching Mode κρίνεται με αυτό το μέτρο.

**ΑΠΑΡΑΒΑΤΟΣ ΚΑΝΟΝΑΣ ΑΠΛΟΤΗΤΑΣ:** Μέγιστο 4 στόχοι ορατοί στην οθόνη ταυτόχρονα — ΠΟΤΕ περισσότεροι, ανεξάρτητα από το πόσους ενεργούς στόχους έχει ο μαθητής. Οι υπόλοιποι με swipe (οριζόντια σελιδοποίηση με ένδειξη κουκκίδων). Σειρά: πρώτα οι high priority. Σε ομαδική συνεδρία: μία «κάρτα» ανά μαθητή, swipe μεταξύ μαθητών — όχι όλοι σε ένα πλέγμα στο κινητό. Κανένας counter, καμία λίστα σημειώσεων, κανένα στοιχείο που δεν χρειάζεται ΑΥΤΗ τη στιγμή. Αν ένα UI στοιχείο δεν βοηθά την καταχώρηση του επόμενου tap, δεν ανήκει στο Teaching Mode.

Η διαχείριση μαθητών, στόχων, γραφημάτων και εκθέσεων υπάρχει κανονικά, αλλά είναι το «γραφείο» της εφαρμογής — εκεί πηγαίνεις όταν προετοιμάζεσαι ή απολογίζεσαι, όχι όταν διδάσκεις.

## Οι 7 οντότητες (data model)

1. **Student** — id, code (π.χ. "Μ1"), nickname (προαιρετικό), grade, notes, functionalProfile (δομημένο — βλ. παρακάτω), preferences (καρτέλα ενισχυτών — βλ. παρακάτω), active

   **functionalProfile:** ανά τομέα, ΟΧΙ ελεύθερο κείμενο αλλά δομημένη λίστα επιλογών (checkboxes) + πεδίο "Παρατηρήσεις". Παράδειγμα για τομέα Επικοινωνία: ☐ μη λεκτικός ☐ μονολεκτικός ☐ φράσεις ☐ προτάσεις ☐ PECS ☐ δείχνει ☐ ζητά βοήθεια ☐ ακολουθεί οδηγίες. Κάθε τομέας έχει τη δική του λίστα επιλογών (ορίζονται σε αρχείο config π.χ. `profileOptions.js` ώστε να επεκτείνονται εύκολα). Αποθήκευση: `{ domain, checkedOptions[], notes }`. Σκοπός: τα δομημένα δεδομένα θα τροφοδοτούν αργότερα στατιστικά και αυτόματες προτάσεις.

   **preferences (καρτέλα ενισχυτών):** έξι δομημένες λίστες, καθεμιά με στοιχεία που προσθέτει ελεύθερα ο εκπαιδευτικός:
   - ❤️ Του αρέσει
   - 🚫 Δεν του αρέσει
   - ⭐ Ενισχυτές
   - 🧩 Αγαπημένα παιχνίδια/δραστηριότητες
   - ⚠️ Triggers
   - 🟢 Τον ηρεμεί
   Εμφανίζεται σε ευδιάκριτο σημείο της καρτέλας μαθητή — είναι καθημερινό εργαλείο, όχι αρχείο.
2. **Goal** — id, studentId, domain (τομέας από σταθερή λίστα), title, description, baseline, criterion (π.χ. "4 από 5 προσπάθειες"), measurementType (successRatio | promptLevel | duration | taskAnalysis), supportLevel, **priority (high | medium | low)**, startDate, status (active | achieved | revised | archived). Οι λίστες στόχων ταξινομούνται/φιλτράρονται με βάση την προτεραιότητα· οι στόχοι υψηλής προτεραιότητας εμφανίζονται πρώτοι στην οθόνη καταγραφής.
3. **ScheduleSlot** — id, dayOfWeek, time, type (individual | group), studentIds[], label
4. **Session** — id, date, slotId (προαιρετικό), studentIds[], status (🟢 completed — ολοκληρώθηκε | 🟡 interrupted — ξεκίνησε αλλά διακόπηκε | 🔴 notHeld — δεν πραγματοποιήθηκε, με πεδίο reason), absentStudentIds[] (η απουσία σημειώνεται ΑΝΑ ΜΑΘΗΤΗ — σε ομαδική συνεδρία μπορεί να λείπει ένα παιδί ενώ η συνεδρία έγινε), **durationMinutes (γρήγορη επιλογή: 5 / 10 / 20 / 40 / 80, με δυνατότητα προσαρμοσμένης τιμής)**, activity, note. Η διάρκεια αθροίζεται σε στατιστικά (πραγματικός χρόνος στήριξης ανά μαθητή/περίοδο)· τα ποσοστά completed/interrupted/notHeld και οι απουσίες τροφοδοτούν την έκθεση.
5. **Measurement** — id, sessionId, studentId, goalId, value (ανάλογα με measurementType), context (individual | group), note
6. **Observation** — id, studentId, date, sessionId (προαιρετικό — αν προέκυψε μέσα σε συνεδρία), text, domain (προαιρετικό), milestone (boolean — «σημαντική στιγμή/πρώτη φορά»). Καταγράφει σημαντικά συμβάντα που ΔΕΝ είναι μετρήσεις στόχων: π.χ. «Πρώτη φορά ζήτησε μόνος του τουαλέτα», «Πρώτη φορά έπαιξε με άλλο παιδί», «Έντονη αισθητηριακή αναζήτηση σήμερα». Κουμπί ➕ Παρατήρηση διαθέσιμο ΠΑΝΤΑ: μέσα στο Teaching Mode και στην καρτέλα μαθητή. Η καταχώρηση πρέπει να παίρνει < 15 δευτερόλεπτα (μαθητής προεπιλεγμένος αν είμαστε σε συνεδρία → κείμενο → προαιρετικά milestone → αποθήκευση). Οι παρατηρήσεις εμφανίζονται στο Timeline του μαθητή και προσφέρονται ως ποιοτικό υλικό στο προσχέδιο έκθεσης.
7. **DomainTemplate** — ένα ανά τομέα: domain, suggestedMeasurementTypes[] (ποιοι τύποι μέτρησης ταιριάζουν στον τομέα), commonCriteria[] (συνηθισμένα κριτήρια, π.χ. "4 από 5 προσπάθειες", "σε 3 συνεχόμενες συνεδρίες"), baselineExamples[] (παραδείγματα διατύπωσης baseline), goalStarters[] (μισοέτοιμες διατυπώσεις στόχων του τομέα). Χρήση: όταν ο εκπαιδευτικός δημιουργεί στόχο και επιλέγει τομέα, η φόρμα-οδηγός προ-συμπληρώνεται με τις επιλογές του template — επιλέγει και προσαρμόζει αντί να γράφει από το μηδέν. Τα templates έρχονται προ-γεμισμένα (seed data σε αρχείο π.χ. `domainTemplates.js`) και είναι επεξεργάσιμα από τον χρήστη μέσα στην εφαρμογή (οι αλλαγές αποθηκεύονται στη βάση). Μαζί με το `profileOptions.js`, αποτελούν το «παιδαγωγικό περιεχόμενο» της εφαρμογής.

Σταθερή λίστα τομέων (domains): Ανάγνωση, Γραφή, Μαθηματικά, Προφορικός λόγος, Επικοινωνία, Κοινωνικές δεξιότητες, Συναισθηματική ανάπτυξη, Αυτοεξυπηρέτηση, Λεπτή κινητικότητα, Αδρή κινητικότητα, Προσοχή/Συγκέντρωση, Εκτελεστικές λειτουργίες, Αισθητηριακές ανάγκες, Συμπεριφορά.

## Τα 6 βήματα υλοποίησης (ΕΝΑ ΚΑΘΕ ΦΟΡΑ)

**Βήμα 1 — Σκελετός + Μαθητές**
Project setup (Vite + React + Dexie + PWA). Λίστα μαθητών, προσθήκη/επεξεργασία/αρχειοθέτηση μαθητή, προφίλ μαθητή. Έλεγχος ότι τα δεδομένα επιβιώνουν μετά από refresh και offline.

**Βήμα 2 — Στόχοι**
Στην καρτέλα μαθητή: προσθήκη στόχων με φόρμα-οδηγό (τομέας → περιγραφή → baseline → κριτήριο → τύπος μέτρησης). Λίστα ενεργών στόχων ανά μαθητή. Αλλαγή κατάστασης στόχου.

**Βήμα 3 — Teaching Mode (ατομικό)**
Το κεντρικό flow όπως περιγράφεται στη «Φιλοσοφία UX»: αρχική οθόνη «Τι κάνεις τώρα;» → Ατομικό → επιλογή μαθητή → Έναρξη → Teaching Mode (μόνο οι ενεργοί στόχοι, καταχώρηση με ένα tap, κουμπί ➕ Παρατήρηση) → Τέλος → διάρκεια → αποθήκευση συνεδρίας. Στόχος UX: όλη η καταγραφή < 60 δευτερόλεπτα.

**Βήμα 4 — Teaching Mode (ομαδικό)**
Ίδιο flow με πολλούς μαθητές: επιλογή με checkboxes → πλέγμα/καρτέλες μαθητών × στόχων μέσα στο Teaching Mode. Δυνατότητα σήμανσης απόντος. Κάθε μέτρηση παίρνει context: "group".

**Βήμα 5 — Πρόοδος + Timeline μαθητή**
(α) Στην καρτέλα κάθε στόχου: γράφημα των μετρήσεων στον χρόνο (recharts), γραμμή κριτηρίου, ένδειξη baseline. Ειδοποίηση στην αρχική αν ένας ενεργός στόχος δεν έχει μέτρηση > 14 μέρες.
(β) **Timeline στην καρτέλα μαθητή:** χρονολογική αφήγηση της χρονιάς, όχι λίστες — π.χ. 📄 Αρχική αξιολόγηση → 🎯 Νέοι στόχοι → 📅 12 συνεδρίες → ⭐ Παρατήρηση-ορόσημο → 📈 Επίτευξη κριτηρίου → 🎯 Αναθεώρηση στόχου → 📄 Έκθεση. Παράγεται αυτόματα από τα υπάρχοντα δεδομένα (στόχοι, συνεδρίες, μετρήσεις, παρατηρήσεις, αλλαγές status) — δεν απαιτεί δική του καταχώρηση. Οι παρατηρήσεις-milestone ξεχωρίζουν οπτικά.

**Βήμα 6 — Προσχέδιο έκθεσης**
Ανά μαθητή: επιλογή περιόδου → η εφαρμογή παράγει κείμενο ανά τομέα/στόχο με templates (baseline, τρέχον επίπεδο, πορεία, επίτευξη κριτηρίου, αριθμός συνεδριών και συνολικός χρόνος στήριξης) → προσφέρει τις παρατηρήσεις της περιόδου (με προτεραιότητα στα milestones) ως ποιοτικό υλικό προς ένταξη → ο εκπαιδευτικός το επεξεργάζεται → export σε .docx ή copy.

## Τι ΔΕΝ φτιάχνουμε στο MVP (μη προστεθούν)

- Λογαριασμοί χρηστών, login, server, συγχρονισμός συσκευών (Φάση 2 — βλ. «Αρχιτεκτονική-στόχος» παραπάνω)
- Portal γονέων/θεραπευτών (μελλοντική φάση — προς το παρόν μόνο μελλοντικό "PDF ενημέρωσης")
- Βιβλιοθήκη δραστηριοτήτων, Teacher Bible
- Εβδομαδιαίο ωρολόγιο/αυτόματη παραγωγή συνεδριών (Phase 2 — στο MVP η συνεδρία δημιουργείται χειροκίνητα)
- AI λειτουργίες
- Πολλαπλές γλώσσες

## Κανόνες συνεργασίας με AI assistant

- Δουλεύουμε ΕΝΑ βήμα τη φορά. Μην υλοποιείς επόμενα βήματα προκαταβολικά.
- Πριν από μεγάλες αλλαγές, εξήγησε σύντομα τι θα κάνεις.
- Κώδικας απλός και σχολιασμένος στα βασικά σημεία — η δημιουργός συντηρεί μόνη της το project.
- Ό,τι κείμενο βλέπει ο χρήστης: στα Ελληνικά.

